package com.kodilla.testing;

import com.kodilla.testing.user.SimpleUser;
import com.kodilla.testing.calculator.Calculator;

public class TestingMain {
    public static void main(String[] args) {
     /*   SimpleUser simpleUser = new SimpleUser("theForumUser");

        String result = simpleUser.getUsername();

        if (result.equals("theForumUser")) {
            System.out.println("test OK");
        } else {
            System.out.println("Error!");
        }
        Calculator calc = new Calculator(3, 1);
        Integer addTwoNo = calc.add();
        Integer subtrackTwoNo = calc.subtrack(); //ok nie ok
        if (addTwoNo == 4) {
            System.out.println("Adding Ok");
        } else {
            System.out.println("Adding error");
        }
        if (subtrackTwoNo.equals(2)) {
            System.out.println("subtracking Ok");
        } else {
            System.out.println("subtracking error");
        } */
    }
}